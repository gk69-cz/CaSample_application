{
  "name": "casample_application-main",
  "version": "1.0.0",
  "description": "A sample Express application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js", // Added for local development with live reload
    "build": "echo \"No build step needed for Express app\""
  },
  "keywords": ["sample", "express", "application"],
  "author": "Christy Zachariah",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22" // Added for local development with auto-reload
  }
}

